function getcomputerchoice(){
    let x=0
    console.log("before")
    while (x=0)
        {
            ifMath.floor(Math.random()*3);
            console.log(x);
        }
    console.log("after" + x)
    
    return x;
}